- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    # git
    ~/.gitconfig: .gitconfig

    # editor
    ~/.vimrc: .vimrc
    ~/.ideavimrc: .ideavimrc

    ~/.tmux.conf: .tmux.conf

# - create:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
