- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    # git
    ~/.gitconfig: .gitconfig

    # editor
    ~/.vimrc: .vimrc
    ~/.ideavimrc: .ideavimrc

# - create:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
